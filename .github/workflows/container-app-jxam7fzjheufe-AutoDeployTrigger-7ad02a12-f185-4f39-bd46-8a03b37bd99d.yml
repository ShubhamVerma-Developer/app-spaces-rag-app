name: Trigger auto deployment for container-app-jxam7fzjheufe

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '//**'
    - '.github/workflows/container-app-jxam7fzjheufe-AutoDeployTrigger-7ad02a12-f185-4f39-bd46-8a03b37bd99d.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPPJXAM7FZJHEUFE_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPPJXAM7FZJHEUFE_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPPJXAM7FZJHEUFE_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }} 
          registryUrl: 
          registryUsername: ${{ secrets.CONTAINERAPPJXAM7FZJHEUFE_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPPJXAM7FZJHEUFE_REGISTRY_PASSWORD }}
          containerAppName: container-app-jxam7fzjheufe
          resourceGroup: appspacesragappae94
          imageToBuild: default/[parameters('containerAppName')]:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


